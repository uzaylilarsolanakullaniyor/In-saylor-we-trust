; Privy (@privy-io/react-auth → permissionless) wants ox@^0.8, while viem
; pulls ox@0.14. The conflict is a peerOptional, so the install is safe —
; we just need npm's strict (v7+) peer resolution to not hard-fail on it.
legacy-peer-deps=true
